apiVersion: v1
kind: Service
metadata:
  labels:
    app: udagram-user
  name: udagram-user
  namespace: udagram
  selfLink: /api/v1/namespaces/udagram/services/udagram-user
spec:
  ports:
  - name: "8080"
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: udagram-user
  sessionAffinity: None
  type: ClusterIP
status:
  loadBalancer: {}
